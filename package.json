{
	"name": "test",
	"version": "1.0.0",
	"description": "This is a simple password generator",
	"main": "server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "nodemon server.js",
		"start": "node server.js"
	},
	"keywords": [
		"use --length=<value> to set length of the password"
	],
	"author": "tekto",
	"license": "ISC",
	"dependencies": {
		"chalk": "^4.1.2",
		"clipboardy": "^2.3.0",
		"commander": "^8.1.0"
	}
}
